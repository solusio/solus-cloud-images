#!/bin/bash

clear_user(){
	for user in debian; do
		userdel --force --remove "$user" || passwd -l "$user" || true
		rm -rf "/home/$user/"
	done
	passwd -d root
}

clean_up(){
	rm -rf /lib/systemd/system/rc-local.service.d
	rm -rf /etc/rc.local
	rm -rf /etc/subgid.lock
}

clear_user

clean_up